# Generated by ts-rs from crates/thunderforge-canvas-core.
#
# Rewritten on every `cargo test` of that crate, not only when
# `pnpm sdk:bindings` runs — so if prettier owned them too, a bare test run
# would leave the tree dirty and `sdk:check` would report drift that no source
# change caused. The generator is the authority on their formatting;
# `pnpm sdk:check` is what guards them against their Rust source.
#
# commands.ts and __tests__/ in the same directory are hand-written and stay
# under prettier.
src/engine/sdk/*.ts
!src/engine/sdk/commands.ts
