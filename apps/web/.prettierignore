# Generated by ts-rs from crates/thunderforge-canvas-core.
#
# Written by `pnpm sdk:bindings`, and compared byte-for-byte with a fresh
# export by `pnpm sdk:check` — so if prettier owned them too, the two would
# fight and the check would report drift that no source change caused. The
# generator is the authority on their formatting; `pnpm sdk:check` is what
# guards them against their Rust source.
#
# commands.ts and __tests__/ in the same directory are hand-written and stay
# under prettier.
src/engine/sdk/*.ts
!src/engine/sdk/commands.ts
